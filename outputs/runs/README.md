# Runs — Rolling Changelog

One line per numbered run. Newest at the bottom. Columns:

| Run | Date | Eval | Hypothesis | Aggregate success | Decision |
|-----|------|------|------------|-------------------|----------|

Keys:
- **keep** — change merged; S1 floor held, targeted scenario improved,
  no regressions elsewhere.
- **revert** — src diff undone; artifacts preserved for reference.
- **inconclusive** — no clear win; see the run's `notes` for next step.

Runs 1–6 were the prior offline LFW pair-verification convention
(deleted — see `docs/prompts/optimize_recognition.md` for the current
synthetic-harness protocol). The live knob `REMATCH_THRESHOLD = 0.60`
introduced in that era is preserved in the current `src/gatekeeper.py`.

See `docs/prompts/optimize_recognition.md` for the iteration protocol
and `outputs/runs/run_NNN.json` for full artifacts.

---

| Run     | Date       | Hypothesis                                                  | Aggregate     | Decision |
|---------|------------|-------------------------------------------------------------|---------------|----------|
| run_007 | 2026-04-18 | synth | Baseline under the new synthetic-harness convention. | 125/150 (83 %) · S3=0 %, others 100 % | keep (baseline) |
| run_008 | 2026-04-18 | synth | Add `S7_hard_pose` + `S8_distance_noise` scenarios; establish pose/distance floors. | 172/200 (86 %) · S7=88 %, S8=100 %, S3=0 %, others 100 % | keep (baseline) |
| run_009 | 2026-04-19 | real  | First real-data baseline (SCface + ChokePoint, full pipeline). | 47/414 (11 %) · **283/414 pairs had NO face detected** — MTCNN floor (`MIN_FACE_PX=60`, conf≥0.90) rejects every d1/d2 SCface + most ChokePoint frames before embedding. d3_close gets 47/126 (37 %) once detection lands. | keep (baseline) |
| run_010 | 2026-04-19 | real  | `MIN_FACE_PX: 60 → 40` (class 4, far-face handling). | 54/414 (13 %) · d1_far 0 %, d2_mid +4.6 %, d3_close +0.8 %, chokepoint 0 %. Targeted scenarios needed +≥20 % absolute; best move was d2_mid at +4.6 %. | **revert** — MIN_FACE_PX isn't the gate; next candidate is MTCNN_CONF_MIN. |
| run_011 | 2026-04-19 | real  | Swap embedder **FaceNet → ArcFace** (buffalo_l / w600k_r50). Bundled: 2pt/160→5pt/112 alignment, thresholds MATCH 0.70→0.40 / REMATCH 0.60→0.30 / ENROLL 0.40→0.20. | 126/414 (30 %) · **d3_close 36.2 % → 96.2 % (+60 pts)**, aggregate +19 pts. d1_far / d2_mid / chokepoint still ≈0 % — size gate MIN_FACE_PX=60 blocks ArcFace from running on those (d1 faces are 18–30 px per diagnostic). | **keep** — definitive embedder win on d3. run_012 should pair with MIN_FACE_PX=40 to unlock d1/d2. |
| run_012 | 2026-04-19 | real  | Under kept ArcFace, `MIN_FACE_PX: 60 → 40`. | 247/414 (60 %) · **d2_mid 0.8 % → 91.5 % (+90.8 pts)**, d3_close 96.2 % → 98.5 % (+2.3), d1_far 0 % (faces 18-30 px, below new floor), chokepoint 0 %. Aggregate +29.3 pts. | **keep** — hypothesis fully validated. ArcFace handles 40 px cleanly where FaceNet couldn't (run_010 was only +4.6 %). |
| run_013 | 2026-04-20 | real  | Chokepoint GT-grounded baseline. Old loader paired frame[25 %] vs frame[75 %] within each session-camera as "one subject" — in reality those are different walkers. Per-frame GT (`<person id=>` + eye coords) now drives pair construction: `temporal_stability` (same person, first-vs-last frame, same camera) + `cross_camera` (same person across 2 cameras of same session). Detection also switched to GT-eye-closest match when `PairSpec` carries eye coords, so we pick the labeled person not the largest face. | 788/1190 (66 %) · d1_far 0 %, d2_mid 92 %, d3_close 98 %, **temporal_stability 57 %** (342/600, 248 skipped on "no face"), **cross_camera 99.5 %**. Temporal's match rate *when detection succeeds* is **97 %** — the 57 % is almost entirely detection-bound at the walk bookends. Cross_camera at ceiling → little headroom there. | **keep (baseline)** — old chokepoint=0 % was apparatus, not model. New baseline for runs 014+. |
| run_014 | 2026-04-20 | real  | `MIN_FACE_PX: 40 → 20` under kept ArcFace. Targets (a) d1_far (faces 18-30 px, 100 % rejected by 40 px gate) and (b) temporal_stability bookend frames (248/600 skipped in run_013). | 1095/1190 (92 %) · **d1_far 0 % → 53 % (+53 pts)**, **temporal_stability 57 % → 95 % (+38 pts)**, d2_mid 92 % → 100 % (+8), d3_close flat 98 %, cross_camera flat 99.5 %. Detection essentially solved (4/130 d1_far skipped, 18/600 temporal skipped). Remaining d1_far failures (61/126 detected) are match-layer, not detection. | **keep** — decisive. Both targets cleared +20 % threshold with zero regressions. `MIN_FACE_PX=20` synced to `server.py` + `live_webcam.py`. Next run pivots to MATCH_THRESHOLD (not the originally-planned MTCNN_CONF_MIN). |
| run_015 | 2026-04-20 | mixed | **Hardened-harness baseline at REMATCH=0.30.** No pipeline knob diff. Harness adds per-scenario **TAR@FAR=1 % / 0.1 %** via exhaustive cross-subject impostor cosines, runs real `quick_quality_check` on every aligned crop (was: hard-coded 0.9 / 0.7), splits ChokePoint into `within_clip_sanity` (renamed from `temporal_stability` — near-duplicate, no longer for decision-making) + `cross_camera` + new `cross_session`, drops the TinyFace stub. | 1157/1315 (88 %) · synth 175/200, real 982/1115 · d1_far sr=53 % **TAR@1 %=98 %** (gap −45), d2_mid sr/TAR 100 %, d3_close sr 98 % TAR 100 %, **within_clip sr=95 % TAR@1 %=31 % (gap +64)**, cross_camera sr=99.5 % TAR@1 %=80 % (gap +20), cross_session sr/TAR 100 % (n=25). S1 floor 100 %. | **keep (baseline)** — audit vindicated: within_clip's 95 % was ~64 pts frame-leakage inflation; d1_far's 53 % is NOT threshold-inflated (TAR@1 %=98 % says embeddings separate cleanly, loss is REMATCH=0.30 rejecting hard genuines). |
| run_016 | 2026-04-20 | mixed | `REMATCH_THRESHOLD: 0.30 → 0.20` under the hardened harness. Re-tries the original run_015-author hypothesis with honest scoring. | 1357/1415 (96 %) · synth 175/200, real 1182/1215 · **d1_far 53 % → 91 % (+38 pts)**, within_clip 95 % → 97 %, others unchanged. **TAR@FAR=1 % identical across every scenario** (98 % d1_far, 31 % within_clip, 80 % cross_camera, 100 % elsewhere) — REMATCH picks an operating point on the same ROC, not a better discriminator. d1_far sr=0.91 vs TAR@0.1 %=0.91 → REMATCH=0.20 corresponds to ~0.1 % FAR on SCface d1_far (tighter than the audit assumed). S1 floor 100 %. | **keep ⚠ compromised by run_017** — passes CLAUDE.md rule (S1≥95 %, d1_far +20 pts abs, zero regressions) under verification (gallery=1) but run_017 exposes a 35 % enrollment dedup collision rate at this REMATCH under gallery=130. See run_018 for revisit. |
| run_017 | 2026-04-20 | identification | Phase 2 hardening: gallery-130 identification on SCface. Enroll every subject's frontal mugshot into one Gatekeeper, query each subject's cam1 image at each distance bucket, report rank-1 gk accept (live-pipeline) + rank-1/5/10 emb (embedder-only). Zero pipeline knob diff from run_016. | 434/449 (97 %) · **enrollment dedup collision: 46/130 subjects (35 %) merge into existing identities at REMATCH=0.20 — gallery seats only 84 uniques**. On the 84 survivors: rank1_gk=94 % / 100 % / 99 % (d1/d2/d3), rank1_emb=96 % / 100 % / 100 %, rank5_emb=99 % / 100 % / 100 %. Embedder gap vs rank1_gk is only 2-3 pts → REMATCH is not the surviving-path bottleneck; **the enrollment collision is the failure mode**. S1 floor 100 %. | **inconclusive (baseline)** — gallery-N baseline, no knob diff. Hard-invalidates run_016\'s gallery-1 91 % headline: in a real multi-identity deployment 35 % of enrolled walkers would be confidently matched to the wrong pre-existing identity. run_018 should revert REMATCH to 0.30, re-measure dedup + rank-k, confirm or rule out the threshold root cause. |
| run_018 | 2026-04-20 | identification | `REMATCH_THRESHOLD: 0.20 → 0.30` under gallery-130 identification. Tests whether reverting REMATCH fixes run_017's 46/130 collision rate without destroying d1_far retrieval. | 359/449 (80 %) · **collisions 46 → 1** (nearly eliminated, hypothesis confirmed on this axis) but **d1_far rank1_gk 94 % → 52 %** (-42 pts; the exact tail-match genuines run_016 was solving are now rejected). 39 new "UNCERTAIN" enrollments emerge as subjects whose cosine lands in the new [0.20, 0.30) band — saved from collision but not seated. rank1_emb unchanged (97 %/100 %/100 %) — embedder is fine; REMATCH picks the operating point. S1 floor 100 %. | **revert** — fails decision rule (d1_far drops > 5 pts). Deeper finding: REMATCH serves two conflicting roles at `gatekeeper.py:161` — strict cross-track dedup (wants ~0.40) and loose re-acquisition (wants ~0.20). One knob can't do both. Architectural fix belongs to run_019 (route first-contact dedup through MATCH_THRESHOLD, keep REMATCH for re-acquisition). |
| run_019 | 2026-04-20 | mixed (synth + verif + ident) | **Architectural split**: add rank-1-vs-rank-2 margin check in `_find_best_match_with_margin`. First-contact now matches iff `score ≥ MATCH_THRESHOLD=0.40` OR (`score ≥ REMATCH_THRESHOLD=0.20` AND `margin ≥ MIN_MATCH_MARGIN=0.05`). Decouples re-acquisition (loose) from dedup (strict) via the rank-1 separation signal. | 1581/1664 (95 %) · **Identification**: collisions 46 → 18 (-61 %), seated 84 → 88 uniques, rank1_gk d1_far 94 % → 93 % (within noise), rank1_emb unchanged (97 %/100 %/100 %). 24 new UNCERTAIN drops (strictly better than silent merges — recoverable with retry). **Verification** unchanged vs run_016 on every scenario (d1_far sr 91 %, TAR@FAR=1 % 98 % on d1_far, within_clip 31 %, cross_camera 80 %, cross_session 100 %). S1 floor 100 %. | **keep** — clears CLAUDE.md rule on all three harness types: S1≥95 %, dedup collision rate 35 %→14 % (-21 pts absolute, targeted scenario), no scenario regresses ≥20 pts. Residual 18 collisions are embedder-intrinsic (some SCface subjects' mugshots are genuinely close in ArcFace space). Next lever would be multi-shot enrollment via `data/real_eval/scface/mugshot_rotation/` L1-L4/R1-R4 variants — out of scope for this run. |
| run_020 | 2026-04-20 | identification (multi-shot) | **Multi-shot enrollment**: each SCface subject seats with 5 pose templates `{frontal, L1, L4, R1, R4}` via two-phase `_enroll_gallery` — Phase 1 seats on frontal only (dedup-safe, matches single-shot semantics), Phase 2 enriches each seated identity's bank via the tracking-lock update path (`_try_update_embeddings`). No pipeline knob changes — pure enrollment protocol. | 375/399 (94 %) · **d1_far rank1_gk 92.9 % → 95.3 % (+2.4 pts)**, d1_far rank1_emb 96.5 % → 97.6 % (+1.1), **d3_close rank1_gk 98.9 % → 100 %** (+1.1). dedup_collisions 18 → 16 (stable; 2-pt shift is file-level noise — `mugshot_rotation/frontal.jpg` ≠ `mugshot/frontal.JPG`). Extras added 343/352 (97 % quality-gate acceptance). S1 floor 100 %. | **keep** — all four near-ceiling gates met (d1_far ≥ +2 pts, no scenario regresses ≥ 5 pts, S1 ≥ 95 %, no collision increase). Two-phase design validated: Phase 2 enrichment lifts retrieval without regressing Phase 1 dedup. Production recommendation: collect 3-5 pose templates per user at enrollment rather than single frontal. Residual 16 collisions are embedder-intrinsic — would need a stronger encoder to move further. |
| run_021 | 2026-04-21 | mixed (synth + verif + ident) | **Drift-break in the tracking-lock path**. On every locked-track frame, compute max cosine between the incoming embedding and the locked identity's bank; if it drops below `REMATCH_THRESHOLD=0.20` the embedding is no longer plausibly the locked identity, so rerun `_find_best_match_with_margin` against the full bank. Re-lock to a different identity iff it passes the run_019 first-contact gate (`score ≥ MATCH_THRESHOLD=0.40` OR `score ≥ REMATCH_THRESHOLD` AND `margin ≥ MIN_MATCH_MARGIN=0.05`); otherwise hold the lock and skip the bank update. Pure behavioural change — no new knob. | 1581/1664 (95 %) · **S3_crossing 0 % → 100 % (+100 pts, primary target)**, all other synth scenarios remain 100 %. **Verification flat vs run_019**: d1_far sr 91 % TAR@1 %=98 %, d2_mid / cross_session 100 %, d3_close sr 98.5 %, within_clip sr 97 % TAR@1 %=31 %, cross_camera sr 99.5 % TAR@1 %=80 %. **Identification flat vs run_020**: rank1_gk d1_far 95.3 % / d2_mid 100 % / d3_close 100 %, collisions 16, 88/130 enrolled. S1 floor 100 %. | **keep** — decisive. Fixes the S3 crossing failure that had sat at 0 % since run_007 without regressing any verification or identification scenario (all within ±0.5 pts of prior-run baselines). Drift-break correctly no-ops on benign cosine drift (S5/S6/S8 at 1.00) and on Phase-2 multi-shot enrichment (collisions unchanged). IoU track-swap mid-crossing is now self-correcting at the Gatekeeper layer rather than silently polluting identity banks. |
